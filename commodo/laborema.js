let m = {};
let t = [1, 2, 3]; // t is an array, which can be treated like a tuple in some contexts
m.tuple = t; // Assigning the array to the tuple property of object m
console.log(m.tuple); // Output: [1, 2, 3]
